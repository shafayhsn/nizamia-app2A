App-2A V58 — Action Engine Upgrade

Scope kept precise, no over-design.

Included:
- Orders: stronger existing action toolbar with Select Filtered, Clear, selected-count, Save View, Saved Views loader.
- Orders: destructive bulk Delete removed from toolbar and replaced with safe Archive Orders.
- Orders: existing Print / Export / Status / Assign / Duplicate actions remain functional through selected rows.
- Queues: existing multi-select queue actions preserved.
- Buyers: added search, sort, multi-select toggle, Select Filtered, selected-count, export selected/filtered, duplicate selected, archive selected.
- Suppliers: added multi-select toggle, Select Filtered, selected-count, export selected/filtered, duplicate selected, archive selected.

Safety:
- No bulk delete action added.
- Archive/deactivate behavior used for safety.
- UI language, spacing and current button system retained.

Build:
- npm run build passed.
