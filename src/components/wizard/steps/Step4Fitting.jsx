// Step4Fitting.jsx
import React from 'react'
export default function Step4Fitting({ orderId }) {
  return (
    <div className="empty-state" style={{ padding: '32px 0' }}>
      <p style={{ fontWeight: 600, color: 'var(--text)' }}>Fitting</p>
      <p>Optional step — record fit block, spec ref, and fitting status.</p>
      <p style={{ fontSize: 11 }}>Full implementation in next build.</p>
    </div>
  )
}
