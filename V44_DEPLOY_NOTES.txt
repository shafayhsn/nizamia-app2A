App-2A v44 Vercel Install Fix

Cause of long Vercel builds:
package-lock.json contained internal OpenAI registry resolved URLs, causing Vercel npm install to hang/time out.

Fix:
- Removed package-lock.json
- Forced public npm registry via .npmrc
- Kept Queue Print Center restore from v43
- No DB changes

Deploy instruction:
Upload this folder/ZIP to GitHub. Vercel will run npm install from public npm registry and then npm run build.
